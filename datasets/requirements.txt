# PyTorch CUDA wheels are not on plain PyPI for Windows — install from this index.
# cu130 (CUDA 13.0) was picked because the installed NVIDIA driver (591.86) reports
# max supported CUDA 13.1; cu130 is the newest build at or below that. Re-check with
# `nvidia-smi` before bumping this if the driver changes.
--extra-index-url https://download.pytorch.org/whl/cu130

torch==2.14.0+cu130
numpy==2.5.3
tensorboard==2.21.0
pytest==9.1.1
